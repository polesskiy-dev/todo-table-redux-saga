const authorization = (authorizationMap, action) => {
    switch (action.type) {
        //TODO: implement authorization reducer

        /** default */
        default:
            return authorizationMap;
    }
};

export default authorization